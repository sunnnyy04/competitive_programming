#include <bits/stdc++.h>
using namespace std;

int main() {
    int t;
    cin >> t;
    while (t--) {
        int n,m;
        cin>>n>>m;
        vector<vector<int> >arr(n,vector<int>(m));
        for(int i=0;i<n;i++){
            for(int j=0;j<n;j++){
                cin>>arr[i][j];
            }
        }
        int rs=0;
        int cs=0;
        int re=n-1,ce=m-1;
        int i=0,j=0;
        while(rs<=re || cs<=ce){
            

        }

 }
}